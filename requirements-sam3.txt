# SAM2 런타임 의존성
# torch/torchvision/huggingface_hub: requirements.txt에서 이미 설치됨 → 중복 제외
# decord: 비디오 디코딩 전용 → 이미지 파이프라인 불필요 → 제외

einops==0.8.2
pycocotools==2.0.11

# sam2 필수 런타임 의존성 (pip install 시 자동 설치되지 않는 항목)
hydra-core==1.3.2
iopath==0.1.10

# sam2 본체: Dockerfile에서 --no-deps로 별도 설치 (torch 중복 방지)
# git+https://github.com/facebookresearch/sam2.git
